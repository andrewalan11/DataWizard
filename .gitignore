# macOS
.DS_Store

# DataWizard commit guard breadcrumb (per-clone, regenerated; never commit)
SYNC-BLOCKED.md

# Seed Sync Log is local only
Seed\ Sync\ Log.md

# Obsidian workspace (per-user, not for sync)
.obsidian/
.trash/

# User-specific config
Vault Config.md
task-manager-config.md

# Telegram harvester artifacts (credentials and session files - never commit)
Scripts/.env
Scripts/*.session
Scripts/output/
Scripts/t/
Scripts/__pycache__/
*.pyc
