---
title: DW Seed Git Setup
type: guide-section
parent: '[[Git Guide]]'
section: 4
created: '2026-05-02'
updated: '2026-08-31'
edit_log:
  - "DW-S318 2026-08-31 - scope warning added: reset safe only in fresh-setup
    context; recovery routes to 7.0"
---

# 4.0 DW Seed Git Setup

Connect your DataWizard Seed folder to the GitHub repo so you can pull updates with a single command. This section is specific to the Seed -- for general vault or project git setup, see Sections 1-3.

## Path A: Fresh Install (No Seed Yet)

If you don't have a Seed folder yet, clone the repo directly:

```bash
cd "/path/to/your/vault"
git clone https://github.com/andrewalan11/DataWizard.git "_DataWizard/Seed"
```

Replace `/path/to/your/vault` with your actual vault path. To find it on Mac: open Finder, navigate to your vault folder, press Cmd+Option+C to copy the full path.

You should see a new `_DataWizard/Seed/` folder in your vault with all the Seed files.

Skip to "Exclude from Parent Vault Git" below.

## Path B: Existing Seed (Already Installed)

If you already have a `_DataWizard/Seed/` folder from the zip install or a previous setup, connect it to git in place:

```bash
cd "/path/to/your/vault/_DataWizard/Seed"
git init
git branch -m master main
git remote add origin https://github.com/andrewalan11/DataWizard.git
git fetch origin main
git reset --hard origin/main
```

What this does: creates a git repo inside your existing Seed folder, points it at the DataWizard repo on GitHub, and syncs your local files to match the latest remote version.

**Note:** `git reset --hard` will overwrite any local changes to Seed files. If you've customized any Seed files (which you normally shouldn't -- user-specific settings go in `Vault Config.md` which is excluded from git), back them up first.

**Scope warning:** this fresh-setup block is the ONLY context where `git reset --hard origin/main` is safe as written -- `origin` was set to the canonical repo two lines above, and the folder has no git history to lose. Never run it as a recovery step on an existing clone (especially one with multiple remotes, or a fork as `origin`) -- use "Recovering a Seed Clone (Remote-Agnostic)" in 7.0 Safety and Recovery instead.

## Exclude from Parent Vault Git

If your vault has its own git repo, tell it to ignore the Seed's nested repo:

```bash
echo "_DataWizard/Seed/" >> "/path/to/your/vault/.gitignore"
```

If the parent vault was already tracking the Seed folder:

```bash
cd "/path/to/your/vault"
git rm -r --cached "_DataWizard/Seed/"
git commit -m "Stop tracking DataWizard Seed (now a nested repo)"
```

If your vault doesn't use git at all, skip this step.

## Pulling Updates

To get the latest Seed version at any time:

```bash
cd "/path/to/your/vault/_DataWizard/Seed"
git pull origin main
```

DataWizard also checks for updates automatically -- when you start a new conversation, Claude compares your local Seed version against GitHub and tells you if an update is available.

## Optional: DW Save Integration

If you use DW Save (the Cmd+Shift+S sync shortcut), it can handle Seed updates as part of its sync cycle. See `_DataWizard/Seed/Scripts/datawizard-sync-setup.md` for setup details.
